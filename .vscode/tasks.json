{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Gateway",
			"type": "shell",
			"command": "KAFKA_BOOTSTRAP_SERVERS=localhost:9092 KAFKA_TOPIC_BLOCKS=blocks KAFKA_TOPIC_TRANSACTIONS=transactions PYTHONPATH=. /usr/bin/python3 -m uvicorn gateway.main:app --host 0.0.0.0 --port 8000",
			"isBackground": true,
			"group": "gateway"
		}
	]
}